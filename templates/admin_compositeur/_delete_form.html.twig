<form method="post" action="{{ path('admin_compositeur_delete', {'id': compositeur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compositeur.id) }}">
    <button class="btn">Delete</button>
</form>
