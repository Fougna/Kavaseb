<form method="post" action="{{ path('admin_studio_edit_delete', {'id': studio_edit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ studio_edit.id) }}">
    <button class="btn">Delete</button>
</form>
