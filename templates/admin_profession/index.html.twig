{% extends 'base.html.twig' %}

{% block title %}Profession index{% endblock %}

{% block body %}
    <h1>Profession index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for profession in professions %}
            <tr>
                <td>{{ profession.id }}</td>
                <td>{{ profession.nom }}</td>
                <td>
                    <a href="{{ path('admin_profession_show', {'id': profession.id}) }}">show</a>
                    <a href="{{ path('admin_profession_edit', {'id': profession.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_profession_new') }}">Create new</a>
{% endblock %}
