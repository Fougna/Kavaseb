{% extends 'base.html.twig' %}

{% block title %}Systeme{% endblock %}

{% block body %}
    <h1>Systeme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ systeme.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ systeme.nom }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ systeme.image }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ systeme.annee ? systeme.annee|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ systeme.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_systeme_index') }}">back to list</a>

    <a href="{{ path('admin_systeme_edit', {'id': systeme.id}) }}">edit</a>

    {{ include('admin_systeme/_delete_form.html.twig') }}
{% endblock %}
