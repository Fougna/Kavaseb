{% extends 'base.html.twig' %}

{% block title %}Preface{% endblock %}

{% block body %}
    <h1>Preface</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ preface.id }}</td>
            </tr>
            <tr>
                <th>Profession</th>
                <td>{{ preface.profession }}</td>
            </tr>
            <tr>
                <th>Importance</th>
                <td>{{ preface.importance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_preface_index') }}">back to list</a>

    <a href="{{ path('admin_preface_edit', {'id': preface.id}) }}">edit</a>

    {{ include('admin_preface/_delete_form.html.twig') }}
{% endblock %}
