{% extends 'base.html.twig' %}

{% block title %}Traducteur{% endblock %}

{% block body %}
    <h1>Traducteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ traducteur.id }}</td>
            </tr>
            <tr>
                <th>Profession</th>
                <td>{{ traducteur.profession }}</td>
            </tr>
            <tr>
                <th>Importance</th>
                <td>{{ traducteur.importance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_traducteur_index') }}">back to list</a>

    <a href="{{ path('admin_traducteur_edit', {'id': traducteur.id}) }}">edit</a>

    {{ include('admin_traducteur/_delete_form.html.twig') }}
{% endblock %}
