{% extends 'base.html.twig' %}

{% block title %}Saison index{% endblock %}

{% block body %}
    <h1>Saison index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Annee</th>
                <th>Numero</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for saison in saisons %}
            <tr>
                <td>{{ saison.id }}</td>
                <td>{{ saison.titre }}</td>
                <td>{{ saison.annee ? saison.annee|date('Y-m-d') : '' }}</td>
                <td>{{ saison.numero }}</td>
                <td>
                    <a href="{{ path('admin_saison_show', {'id': saison.id}) }}">show</a>
                    <a href="{{ path('admin_saison_edit', {'id': saison.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_saison_new') }}">Create new</a>
{% endblock %}
