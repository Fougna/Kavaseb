{% extends 'base.html.twig' %}

{% block title %}Episode{% endblock %}

{% block body %}
    <h1>Episode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ episode.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ episode.numero }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ episode.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_episode_index') }}">back to list</a>

    <a href="{{ path('admin_episode_edit', {'id': episode.id}) }}">edit</a>

    {{ include('admin_episode/_delete_form.html.twig') }}
{% endblock %}
