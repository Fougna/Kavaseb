{% extends 'base.html.twig' %}

{% block title %}Realisateur index{% endblock %}

{% block body %}
    <h1>Realisateur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Profession</th>
                <th>Importance</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for realisateur in realisateurs %}
            <tr>
                <td>{{ realisateur.id }}</td>
                <td>{{ realisateur.profession }}</td>
                <td>{{ realisateur.importance }}</td>
                <td>
                    <a href="{{ path('admin_realisateur_show', {'id': realisateur.id}) }}">show</a>
                    <a href="{{ path('admin_realisateur_edit', {'id': realisateur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_realisateur_new') }}">Create new</a>
{% endblock %}
