{% extends 'base.html.twig' %}

{% block title %}Realisateur{% endblock %}

{% block body %}
    <h1>Realisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ realisateur.id }}</td>
            </tr>
            <tr>
                <th>Profession</th>
                <td>{{ realisateur.profession }}</td>
            </tr>
            <tr>
                <th>Importance</th>
                <td>{{ realisateur.importance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_realisateur_index') }}">back to list</a>

    <a href="{{ path('admin_realisateur_edit', {'id': realisateur.id}) }}">edit</a>

    {{ include('admin_realisateur/_delete_form.html.twig') }}
{% endblock %}
