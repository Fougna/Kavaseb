{% extends 'base.html.twig' %}

{% block title %}Auteur{% endblock %}

{% block body %}
    <h1>Auteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ auteur.id }}</td>
            </tr>
            <tr>
                <th>Profession</th>
                <td>{{ auteur.profession }}</td>
            </tr>
            <tr>
                <th>Importance</th>
                <td>{{ auteur.importance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_auteur_index') }}">back to list</a>

    <a href="{{ path('admin_auteur_edit', {'id': auteur.id}) }}">edit</a>

    {{ include('admin_auteur/_delete_form.html.twig') }}
{% endblock %}
