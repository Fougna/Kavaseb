{% extends 'base.html.twig' %}

{% block title %}EditeurFrancais index{% endblock %}

{% block body %}
    <h1>EditeurFrancais index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Profession</th>
                <th>Importance</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for editeur_francai in editeur_francais %}
            <tr>
                <td>{{ editeur_francai.id }}</td>
                <td>{{ editeur_francai.profession }}</td>
                <td>{{ editeur_francai.importance }}</td>
                <td>
                    <a href="{{ path('admin_editeur_francais_show', {'id': editeur_francai.id}) }}">show</a>
                    <a href="{{ path('admin_editeur_francais_edit', {'id': editeur_francai.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_editeur_francais_new') }}">Create new</a>
{% endblock %}
