{% extends 'base.html.twig' %}

{% block title %}Scenariste index{% endblock %}

{% block body %}
    <h1>Scenariste index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Profession</th>
                <th>Importance</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for scenariste in scenaristes %}
            <tr>
                <td>{{ scenariste.id }}</td>
                <td>{{ scenariste.profession }}</td>
                <td>{{ scenariste.importance }}</td>
                <td>
                    <a href="{{ path('admin_scenariste_show', {'id': scenariste.id}) }}">show</a>
                    <a href="{{ path('admin_scenariste_edit', {'id': scenariste.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_scenariste_new') }}">Create new</a>
{% endblock %}
