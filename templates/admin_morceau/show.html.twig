{% extends 'base.html.twig' %}

{% block title %}Morceau{% endblock %}

{% block body %}
    <h1>Morceau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ morceau.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ morceau.type }}</td>
            </tr>
            <tr>
                <th>TitreOriginal</th>
                <td>{{ morceau.titreOriginal }}</td>
            </tr>
            <tr>
                <th>TitreFrancais</th>
                <td>{{ morceau.titreFrancais }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ morceau.numero }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_morceau_index') }}">back to list</a>

    <a href="{{ path('admin_morceau_edit', {'id': morceau.id}) }}">edit</a>

    {{ include('admin_morceau/_delete_form.html.twig') }}
{% endblock %}
