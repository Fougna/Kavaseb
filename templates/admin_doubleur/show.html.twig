{% extends 'base.html.twig' %}

{% block title %}Doubleur{% endblock %}

{% block body %}
    <h1>Doubleur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ doubleur.id }}</td>
            </tr>
            <tr>
                <th>Profession</th>
                <td>{{ doubleur.profession }}</td>
            </tr>
            <tr>
                <th>Importance</th>
                <td>{{ doubleur.importance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_doubleur_index') }}">back to list</a>

    <a href="{{ path('admin_doubleur_edit', {'id': doubleur.id}) }}">edit</a>

    {{ include('admin_doubleur/_delete_form.html.twig') }}
{% endblock %}
