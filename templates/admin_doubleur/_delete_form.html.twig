<form method="post" action="{{ path('admin_doubleur_delete', {'id': doubleur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ doubleur.id) }}">
    <button class="btn">Delete</button>
</form>
