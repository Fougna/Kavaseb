<form method="post" action="{{ path('admin_film_delete', {'id': film.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette entrée ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ film.id) }}">
    <button class="btn">Supprimer</button>
</form>