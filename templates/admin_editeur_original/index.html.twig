{% extends 'base.html.twig' %}

{% block title %}EditeurOriginal index{% endblock %}

{% block body %}
    <h1>EditeurOriginal index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Profession</th>
                <th>Importance</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for editeur_original in editeur_originals %}
            <tr>
                <td>{{ editeur_original.id }}</td>
                <td>{{ editeur_original.profession }}</td>
                <td>{{ editeur_original.importance }}</td>
                <td>
                    <a href="{{ path('admin_editeur_original_show', {'id': editeur_original.id}) }}">show</a>
                    <a href="{{ path('admin_editeur_original_edit', {'id': editeur_original.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_editeur_original_new') }}">Create new</a>
{% endblock %}
