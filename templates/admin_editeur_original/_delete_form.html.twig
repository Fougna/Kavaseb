<form method="post" action="{{ path('admin_editeur_original_delete', {'id': editeur_original.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ editeur_original.id) }}">
    <button class="btn">Delete</button>
</form>
