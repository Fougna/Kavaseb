{% extends 'base.html.twig' %}

{% block title %}StudioJeu{% endblock %}

{% block body %}
    <h1>StudioJeu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ studio_jeu.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ studio_jeu.nom }}</td>
            </tr>
            <tr>
                <th>Alias</th>
                <td>{{ studio_jeu.alias ? studio_jeu.alias|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ studio_jeu.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_studio_jeu_index') }}">back to list</a>

    <a href="{{ path('admin_studio_jeu_edit', {'id': studio_jeu.id}) }}">edit</a>

    {{ include('admin_studio_jeu/_delete_form.html.twig') }}
{% endblock %}
